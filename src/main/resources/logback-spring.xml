<?xml version="1.0" encoding="UTF-8"?>



<configuration>
    <appender name="Console"
              class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %black(%d{ISO8601}) %highlight(%-5level) [%blue(%t)] %yellow(%C{1.}): %msg%n%throwable
            </Pattern>
        </layout>
    </appender>
    <springProfile name="local">
        <root level="info">
            <appender-ref ref="Console"/>
        </root>
    </springProfile>
    <springProfile name="demo,prod">
        <appender name="cloud-watch" class="io.github.dibog.AwsLogAppender">
            <createLogGroup>false</createLogGroup>
            <groupName>ZaplacRecepte</groupName>
            <streamName>ZaplacRecepte-stream</streamName>
            <dateFormat>yyyyMMdd_HHmm</dateFormat>
            <layout>
                <pattern>[%thread] %-5level %logger{35} - %msg %n</pattern>
            </layout>
        </appender>
        <root level="info">
            <appender-ref ref="cloud-watch"/>
            <appender-ref ref="Console"/>
        </root>
    </springProfile>
</configuration>